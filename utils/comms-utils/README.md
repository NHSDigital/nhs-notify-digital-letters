# DO NOT MERGE

This is a workaround for an issue with the upstream `@nhsdigital/nhs-notify-event-schemas-status-published` package (it has a dependency on the unpublished `@comms/utils` package).

The problem should be resolved by fixing the upstream package, rather than this hack. It is intended only to allow me to make progress in parallel to the proper fix.
