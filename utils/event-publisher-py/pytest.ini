[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = event_publisher/__tests__
addopts =
    -v
    --tb=short
    --strict-markers

[tool:pytest]
testpaths = event_publisher/__tests__
